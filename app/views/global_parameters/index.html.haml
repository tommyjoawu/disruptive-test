%h1 Listing global_parameters

%table
  %thead
    %tr
      %th Description
      %th Code
      %th
      %th
      %th

  %tbody
    - @global_parameters.each do |global_parameter|
      %tr
        %td= global_parameter.description
        %td= global_parameter.code
        %td= link_to 'Show', global_parameter
        %td= link_to 'Edit', edit_global_parameter_path(global_parameter)
        %td= link_to 'Destroy', global_parameter, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Global parameter', new_global_parameter_path
